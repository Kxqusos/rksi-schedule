[project]
name = "schedule-rks"
version = "0.1.0"
description = "Schedule RKS MVP workspace"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["apps/api"]

[tool.pytest.ini_options]
testpaths = ["apps/api/tests"]
pythonpath = ["apps/api"]

